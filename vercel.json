{
  "buildCommand": "pnpm --filter @workspace/nazar run build",
  "outputDirectory": "artifacts/nazar/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
